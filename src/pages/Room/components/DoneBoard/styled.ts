import styled from "styled-components";

const Action = styled.div`
  width: fit-content;

  font-weight: 700;
  background-color: white;

  padding: 5px;
  border-radius: 10px;
  margin-top: 2em;
`;

export default {
  Action,
};
