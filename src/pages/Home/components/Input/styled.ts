import { Input } from "@chakra-ui/react";
import styled from "styled-components";

const WhiteBackgroundInput = styled(Input)`
  background-color: white !important;
`;

export default {
  WhiteBackgroundInput,
};
